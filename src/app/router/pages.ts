import { TPageNames } from "../../interfaces/interfaces";

const Pages: TPageNames = {
  INDEX: 'index',
  PRODUCT: 'product',
  NOT_FOUND: 'not-found'
}

const ID_SELECTOR = '{id}';

export {Pages, ID_SELECTOR};